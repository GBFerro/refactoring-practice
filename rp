#!/usr/bin/env bash
# The repository's front door. Everything is `./rp <command>`.
set -euo pipefail
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$here/node_modules/.bin/tsx" "$here/scripts/rp.ts" "$@"
