/** A Beckworth Music School enrolment for one student, for one term. */
export abstract class Enrolment {
  protected constructor() {
    // Every subclass still does its own setup below - nothing is shared yet.
  }
}
